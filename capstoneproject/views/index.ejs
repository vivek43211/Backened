<!DOCTYPE html>
<html>

<head>
  <title>Lisper</title>
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Gloria+Hallelujah&family=Titan+One&display=swap"
    rel="stylesheet">
  <style>
    body { font-family: Arial, sans-serif; text-align: center; margin-top: 50px; }
    select, button { padding: 10px; font-size: 16px; margin-top: 10px; }
    .response { margin-top: 20px; padding: 10px; border: 1px solid #ddd; display: inline-block; }
    img { max-width: 300px; margin-top: 10px; }
</style>

</head>

<body>
    <form action="/fetch" method="POST">
        <select name="apiType">
            <option value="joke">Joke API</option>
            <option value="dogs">Dogs API</option>
            <option value="cats"> HTTPS Cats API</option>
        </select>
        <button type="submit">Fetch Data</button>
    </form>

    <% if (error) { %>
        <p style="color: red;"><%= error %></p>
    <% } %>

 <% if (locals.data) { %>
    <h1>API Response :</h1>
    <% if (apiType == "joke") { %>
        <div>
            <p>
                <%= locals.data.setup %>
              </p>
        </div>
          <p >
            <%= locals.data.delivery %>
          </p>
    <% } %>
    <% if (apiType == "dogs") { %>
        <img src="<%= data.message %>" alt="">
    <% } %>
    <% if (apiType == "cats") { %>
        <img src="<%= data %>" alt="">
    <% } %>
 <% } %>
 
</body>

</html>